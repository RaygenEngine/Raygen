#include "pch.h"

#include "OculusHMD.h"

namespace Input
{
	//void OculusHMD::OnOculusTrackingStateChanged(const COculusTrackingStateChangedEvent& event)
	//{
	//	memcpy(&headLocalOrientation, &event.head.orientation, 4 * sizeof(float));
	//	memcpy(&headLocalTranslation, &event.head.position, 3 * sizeof(float));
	//	memcpy(&eyeLocalOrientation[ET_LEFT], &event.leftEye.orientation, 4 * sizeof(float));
	//	memcpy(&eyeLocalTranslation[ET_LEFT], &event.leftEye.position, 3 * sizeof(float));
	//	memcpy(&eyeLocalOrientation[ET_RIGHT], &event.rightEye.orientation, 4 * sizeof(float));
	//	memcpy(&eyeLocalTranslation[ET_RIGHT], &event.rightEye.position, 3 * sizeof(float));
	//}
}
